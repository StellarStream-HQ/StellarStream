# In-Progress Update (during migration)

> **Status update: upgrade complete — migrating streams**

The contract upgrade succeeded. We are now migrating streams to V2 in batches. Most
users won't notice anything; if you use the app during this window you may see a
"Migrate my stream" prompt. We'll confirm completion shortly.

— The StellarStream Team
